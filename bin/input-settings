#!/usr/bin/env bash

(
    export DISPLAY=":0"
    export XAUTHORITY="/home/dimas/.Xauthority"

    # swap escape and caps lock
    setxkbmap -option caps:swapescape
    # keyboard key press repeat rate
    xset r rate 300 32

    ## Disable mouse acceleration
    #xinput set-prop 'pointer:Logitech M585/M590' 'libinput Accel Speed' -1
    ## Set mouse sensitivity
    #sen=1.5
    #xinput set-prop 'pointer:Logitech M585/M590' \
    #    'Coordinate Transformation Matrix' $sen 0 0 0 $sen 0 0 0 1
) &
